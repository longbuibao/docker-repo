sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t longbuibao/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true longbuibao/react-test npm test

after_success:
  - docker build -t longbuibao/multi-client ./client
  - docker build -t longbuibao/multi-nginx ./nginx
  - docker build -t longbuibao/multi-server ./server
  - docker build -t longbuibao/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push longbuibao/multi-client
  - docker push longbuibao/multi-nginx
  - docker push longbuibao/multi-server
  - docker push longbuibao/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'us-east-1'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-us-east-1-923445559289'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
